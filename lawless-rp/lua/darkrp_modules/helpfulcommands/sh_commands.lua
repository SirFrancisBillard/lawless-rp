DarkRP.declareChatCommand{
	command = "help",
	description = "Need help?",
	delay = 1,
}

DarkRP.declareChatCommand{
	command = "bored",
	description = "Some things to do when bored.",
	delay = 1,
}

DarkRP.declareChatCommand{
	command = "rules",
	description = "Read the OOC rules of the server.",
	delay = 1,
}

DarkRP.declareChatCommand{
	command = "laws",
	description = "Read the default IC laws.",
	delay = 1,
}
